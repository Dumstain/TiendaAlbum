<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tiendaalbum">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TiendaAlbum">

        <!-- Otras actividades -->
        <activity android:name=".activity.CatalogoStickersActivity" />
        <activity android:name=".activity.ComprarSobreActivity" />
        <activity android:name=".activity.ComprarAlbumActivity" />
        <activity android:name=".activity.RegisterActivity" />

        <!-- Configurar LoginActivity como la actividad de inicio -->
        <activity android:name=".activity.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Declarar MainActivity sin el intent-filter de lanzamiento -->
        <activity android:name=".activity.MainActivity" />

    </application>

</manifest>
